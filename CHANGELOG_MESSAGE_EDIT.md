# تعديل الرسائل في المحادثات - Message Edit Feature

## التاريخ: 11 مايو 2026

## الوصف
تم إضافة ميزة تعديل الرسائل في نظام المحادثات مع قيود زمنية (24 ساعة من وقت الإرسال).

## الملفات المعدلة

### 1. API Service
**الملف:** `src/features/chat/services/chatApi.js`
- إضافة دالة `editMessage` للتواصل مع الباك إند

### 2. Utilities
**الملف:** `src/features/chat/utils/chatUtils.js`
- إضافة `canEditMessage`: للتحقق من إمكانية تعديل الرسالة
- إضافة `getTimeLeftToEdit`: لحساب الوقت المتبقي للتعديل

### 3. Custom Hook
**الملف الجديد:** `src/features/chat/hooks/useEditMessageMutation.js`
- Hook للتعامل مع عملية التعديل
- تحديث الـ cache تلقائياً
- معالجة الأخطاء وعرض رسائل مناسبة

### 4. Message Component
**الملف:** `src/features/chat/components/MessageItem.jsx`
- إضافة زر التعديل (يظهر عند hover على الرسالة)
- واجهة تعديل مع textarea وأزرار حفظ/إلغاء
- عرض الوقت المتبقي للتعديل أسفل الرسالة
- أيقونة ساعة توضح المدة المتبقية

### 5. Messages List
**الملف:** `src/features/chat/components/MessagesList.jsx`
- تمرير `conversationId` إلى `MessageItem`

### 6. Chat Page
**الملف:** `src/features/chat/components/ChatPage.jsx`
- تمرير `conversationId` إلى `MessagesList`

## المميزات

### 1. التحقق من الصلاحيات
- يمكن للمستخدم تعديل رسائله فقط
- لا يمكن تعديل رسائل الآخرين

### 2. القيود الزمنية
- يمكن التعديل خلال 24 ساعة فقط من وقت الإرسال
- عرض الوقت المتبقي بشكل واضح (ساعات ودقائق)

### 3. واجهة المستخدم
- زر التعديل يظهر عند hover على الرسالة
- واجهة تعديل سهلة مع textarea
- أزرار واضحة للحفظ والإلغاء
- عداد الوقت المتبقي أسفل الرسالة

### 4. التجربة
- تحديث فوري للرسالة بعد التعديل
- رسائل نجاح/خطأ واضحة
- تعطيل الأزرار أثناء الحفظ

## رسائل الخطأ

### من الباك إند
- `"Message can only be edited within 24 hours of sending"` → "لا يمكن تعديل الرسالة بعد مرور 24 ساعة"
- `"You can only edit your own messages"` → "يمكنك تعديل رسائلك فقط"

### من الفرونت إند
- رسالة نجاح: "تم تعديل الرسالة بنجاح"
- رسالة خطأ عامة: "فشل تعديل الرسالة"

## كيفية الاستخدام

1. **عرض زر التعديل:**
   - مرر الماوس على رسالتك الخاصة
   - سيظهر زر قلم صغير في أسفل الرسالة

2. **تعديل الرسالة:**
   - اضغط على زر القلم
   - ستظهر textarea لتعديل النص
   - عدل النص كما تريد
   - اضغط على زر الحفظ (✓) أو الإلغاء (✗)

3. **الوقت المتبقي:**
   - يظهر أسفل الرسالة مع أيقونة ساعة
   - يعرض الوقت المتبقي بالساعات والدقائق

## التقنيات المستخدمة
- React Query للـ caching والتحديثات
- React Hooks للـ state management
- Phosphor Icons للأيقونات
- Framer Motion للـ animations
- React Hot Toast للإشعارات

## الاختبار
للاختبار:
1. أرسل رسالة في أي محادثة
2. مرر الماوس على الرسالة
3. اضغط على زر التعديل
4. عدل النص واحفظ
5. تحقق من التحديث الفوري
6. جرب التعديل بعد 24 ساعة (يجب أن يفشل)
